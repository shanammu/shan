#include<stdio.h>
#include<conio.h>
void main()
{
int a,b,count=0;
clrscr();
scanf("%d %d",&a,&b);
printf("the range is %d and %d",a,b);
for(i=a;i<=b;i++)
{
if((i%i==0)&&(i%1==0))
{
count++;
}
}
printf("%d",count);
getch();
}
